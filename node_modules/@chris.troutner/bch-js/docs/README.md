This directory will contain the compiled documentation. To generate the documentation run:

`npm run docs`

You can then open the generated `index.html` file in a web browser to see the generated documentation.
